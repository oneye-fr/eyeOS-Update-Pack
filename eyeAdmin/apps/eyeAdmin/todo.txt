ToDo:
=====
	Check all files, not checked yet.
	Recheck all files and rewrite some parts.
	Add language support for all strings and write official translation files
	Include (only!) full translations into each release
	Redesign the style of the groups and users tab
	Add the links tab as default
	Redesign the multi groups selection
	Add a function to check is there newer version available (using the sf.net pages!)   by vilu and lars-sh
	Add theme selection function for each user and a default tab!   by HarimaKenji and lars-sh

	Later:
	------
		Add a tab for system configuration files
		Add a tab for creating translations
		Install, update(!) and delete apps support
		Use a left sidebar for easier using more tabs! (like Linux)